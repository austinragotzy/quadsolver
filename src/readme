This fold contain all the source code for the quadsolver.

main -the main code.

printout -print the output to see if it has 0,1 or 2 roots and check the accuracy.

readin -to read the 3 inputs a, b and c.

solve -to get the output and return the number of roots.

validatein -to see if the input is valid.

validateout -to see if the output is valid.