#include <stdlib.h>
#include <stdio.h>

int readin(double *a, double *b, double *c){
printf("ax^2+bx+c\na > ");
scanf("%lf", a);
printf("\nb > ");
scanf("%lf", b);
printf("\nc > ");
scanf("%lf", c);
return 0;
}
